/* Pagination Styles - Add before @media query */
.pagination {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 20px;
  margin-top: 30px;
  padding: 20px;
  background: #f8f9fa;
  border-radius: 12px;
}

.pagination-info {
  color: #555;
  font-weight: 600;
  font-size: 0.95rem;
}

.pagination-controls {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
  justify-content: center;
}

.pagination-controls .btn {
  min-width: 45px;
  padding: 8px 12px;
  background: white;
  border: 2px solid #e0e0e0;
  color: #667eea;
  cursor: pointer;
  transition: all 0.3s ease;
}

.pagination-controls .btn:hover:not(:disabled) {
  background: #667eea;
  color: white;
  border-color: #667eea;
  transform: translateY(-2px);
}

.pagination-controls .btn.active {
  background: #667eea;
  color: white;
  border-color: #667eea;
  font-weight: 700;
}

.pagination-controls .btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
  transform: none;
}

/* Add inside @media query at the end */
  .pagination-controls .btn {
    min-width: 40px;
    padding: 6px 10px;
    font-size: 0.9rem;
  }

